package commands;
/** This class is enum of 
 * command's name 
 * which I want to read */
public enum Order {
    CREATE,
    USE,
    INSERT, 
    SELECT, 
    DELETE, 
    UPDATE,
    EXIT
}